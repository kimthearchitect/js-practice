//Q.4 arrayの各要素のに変更を加える (forEach)
//['a', 'b']の各要素にindex値を足した文字列を出力してくださいe.g 'a0'と'b1'

const array = ['a', 'b']
array.forEach((item, index) => {
  console.log(item + index)
})

console.log(array + index)
